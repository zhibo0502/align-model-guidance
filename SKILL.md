---
name: align-model-guidance
description: 按指定模型的官方 guidance 逐项审查并调整项目的 agent 指令、skills、prompt 与相关说明，交付有固定分母和证据的对齐覆盖率。用于模型指导对齐、指令清理或迁移审计；不因对齐文档自动切换生产模型。
---

# 官方模型指导逐项对齐

把当前官方指导转为适合目标项目的最小改动，并让每项判断可复核。适用于不同模型、供应商和项目；不内置某个模型的条目数量、参数、路径或测试配额。

## 目标与执行边界

从请求和上下文确定目标模型、项目根、只审查/修改模式，以及是否包含运行模型迁移。用户指定模型时保留精确名称；只说“最新”才查证并记录当前解析结果。能查证的常规细节直接处理，仅询问无法确定且会实质改变结果的事项。

“清理、调整、对齐”包含范围内本地修改与非破坏验证；“只审阅、不改”保持只读。文档对齐不授权切换生产模型、改变付费档位、购买额度、发布或部署。复用任务已有的精确授权，等待只阻塞依赖该决定的步骤。新消息中的旁问或修正不取消原目标，不因自行设定的时间门槛留下已授权工作。

用户指令优先于本 skill 的建议；本 skill 若导致暂停、询问或留项，链接实际读取的文件并引用对应条款，区分明确限制与自身解释。不将示例建议升级为项目必须具备的能力。

## 获取并固定官方依据

先搜索目标模型的官方 model guidance，再打开正文。OpenAI 模型优先使用当前可用的 OpenAI Docs skill/官方文档工具；不可用时直接搜索供应商官方文档。其他供应商使用其官方模型和迁移文档，不能套用 OpenAI 参数。具体模型指导优先于笼统旧教程。

记录实际模型、官方页面 URL、读取日期及可用的版本/锚点。动态 latest 页面须核对正文确实属于目标模型。无法取得完整资料时标明缺页/旧版本，继续能证明的部分，不补写未知指导或声称全覆盖。已有同模型、同页面的本轮完整读取可复用。

逐段检查用户要求范围内的官方正文，将独立建议、限制、迁移要求和条件能力各列为条目；为每项保留来源段落定位。重复项合并时保留全部来源映射，独立限制不为缩小分母而打包。用“官方章节/段落 → 条目 ID”反查遗漏，而不是只计算自己写出的条目。链接页面仅在当前适用性或实施取决于其中细则时继续读取，不把官方全站变成范围。

## 盘点项目的指令入口

读取适用 AGENTS.md 和已有差异，明确实际 Git 根及仓库外控制入口。默认覆盖项目拥有的指令和配置；全局个人 skills、安装插件、生产会话/记忆及其他项目需明确纳入才修改。

从实际加载关系检查：AGENTS/CLAUDE 等入口、项目 SKILL 及引用、模型配置、人格/工作流说明、代码中的说明性 prompt、生成这些内容的模板或脚本、仍被引用的旧交接文档。跟踪文件清单之外，补查约定指令目录中的未跟踪文件；记录外部引用和无法读取项。不要只搜索模型名字。

固定本轮候选文件清单和基线提交；按需记录内容哈希及命中行。依赖、二进制、历史记录和生成物分别标记，不将排除算通过。历史文档里的命令是原任务数据；优先修正当前入口和加载路径，避免抹去历史证据或制造第二套维护副本。

## 判定与修改

使用 [覆盖率与证据约定](references/coverage.md) 建立轻量条目表和文件清单。每项区分：已符合、已修正并核验、有依据的不适用、明确保留的例外、待处理、未知。能力介绍可以不适用；不适用必须由项目现状和本次范围支持，不能用来掩盖未查明或未完成。

优先删除冲突、过时、重复的指令，再简化真正需要保留的规则。业务不变量、凭据边界和用户授权不会因模型更主动而失效。集中修正权威入口，相关文件引用它；不要把整份官方提示词复制进每个 skill。

针对实际取得的指导，检查主动完成/澄清、指令优先级、写作风格、委派策略、验证强度等适用行为。以任务目标和限制描述期望，不规定每个小任务都规划、委派、全测或产生固定数量文件。仅在宿主允许、子任务可独立且有收益时委派，同时推进其他有效工作。

API 参数、协议、缓存、reasoning 和费用要求与文档行为分开判定。没有迁移授权时记录适用条件与现有模型证据，不批量改模型名，也不删除其他模型仍合法的参数。实施迁移时另按实际官方兼容规则验证，不能把文档覆盖率当作运行验收。

## 验证与交付

读回最终差异，检查相对链接、入口加载关系、样例与实际接口，以及修改过的指令是否仍互相冲突。文字改动以这些检查为主；代码内 prompt 修改复用有意义的相关验证，不为改字新增镜像式测试。通过后仅因新变化、失败或未解决疑点扩大检查；不自动调用收费模型。

逐项绑定最终文件位置、处置理由和验证证据。报告条目判定、适用项对齐、文件筛查、重点语义审阅四种覆盖率，并列出缺口；不把扫描、排除、条件说明或结构检查说成真实模型效果。没有运行实验就明确未验证模型行为改善。

复用项目现有报告格式。若需要以后复核或随提交交付，把脱敏清单、固定范围和必要重算方法放入项目可版本化位置，不能只引用机器上的临时产物。一次性小任务用表即可，不新建长期扫描平台。提交、推送、部署按各自授权执行，最终简明说明成果与未完成状态。

本 skill 的行为设计已于 2026-09-08 对照 [GPT-6 Astra prompting guidance](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices)；该链接是设计依据，不替代未来执行时对指定模型的官方查证。
